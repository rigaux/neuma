 \paragraph*{Départs {{annee_courante}}}
 
  \begin{enumerate}
     {% for m in departs %}
      \item 
     {{m.prenom}} {{m.nom}} ({{m.statut}}, {{m.equipe}})
   {% endfor %}
\end{enumerate}
  
 \paragraph*{Arrivées {{annee_courante}}}
 
 \begin{enumerate}
  \item \textbf{Permanents}\,: 
    {% for m in arrivees %} {% if m.permanent == '1' %}  {{m.prenom}} {{m.nom}} ({{m.statut}}, {{m.equipe.acronyme}}),
    {% endif %}  {% endfor %}
   \item \textbf{Doctorants}\,: 
        {% for m in arrivees %} {% if m.statut == 'Doctorant' %} {{m.prenom}} {{m.nom}} ({{m.equipe.acronyme}}), 
           {% endif %}  {% endfor %}
    \item \textbf{Autres}\,: 
          {% for m in arrivees %}   {% if m.statut != 'Doctorant' and  m.permanent == '0' %}  {{m.prenom}} {{m.nom}} ({{m.statut}}, {{m.equipe.acronyme}}), 
           {% endif %}  {% endfor %}
 \end{enumerate}
 